namespace Lesson_3
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //List<int> list = new List<int> { 2, 3, 3, 4, 1, 2, 5 };
            //List<int> distinct = removeDuplicates(list);

            //Console.WriteLine(String.Join(",", distinct));
            //Bits bits = new Bits();

            //for (int i = 0; i < 7; i++) 
            //{
            //    Console.WriteLine(bits.GetBit(3, i));
            //}
            //Console.WriteLine();

            //Console.WriteLine(bits.SetBit(3, 1, true));
            //Console.WriteLine(bits.SetBit(3, 1, false));
            
            Console.ReadLine();
        }
    }
}